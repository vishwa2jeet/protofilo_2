import { Cousine, Gabarito } from "next/font/google";

export const test = Gabarito({
  weight: "500",
  subsets: ["latin"],
});

export const cousine = Cousine({
  weight: "400",
  subsets: ["greek"],
});
